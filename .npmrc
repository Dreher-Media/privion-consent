# Default access for `npm publish` / `pnpm publish` when no flag is passed.
# Our publish.yml also passes `--access public` explicitly so this is mostly
# a safety net for local publishes.
access=public
